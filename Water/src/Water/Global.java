package Water;

/**
 * This is a temporary global class that has a user.
 * This is only temporary and will change when we actually hand in 
 * the project. This was simply so we could have one user that can be
 * accessed throughout all of the classes in order to save the 
 * data that is input throughout the program. 
 * 
 * @author Sand
 *
 */
public interface Global {
	//public User currentUser=new User();
}
